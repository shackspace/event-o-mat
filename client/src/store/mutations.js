const mutations = {
	SET_USER (state, user) {
		state.user = user
	},
	SET_ROOMS (state, rooms) {
		state.rooms = rooms
	},
	SET_EVENTS (state, events) {
		state.events = events
	}
}

export default mutations
